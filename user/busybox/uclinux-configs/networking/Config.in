###
### DO NOT EDIT THIS FILE; IT IS AUTO-GENERATED
### SEE kconfig-to-uclinux.awk FOR MORE INFO
###

comment '--###-- NETWORKING UTILITIES --###--'
 bool ' Enable IPv6 support' CONFIG_USER_BUSYBOX_FEATURE_IPV6 n
 bool ' arp' CONFIG_USER_BUSYBOX_ARP n
 bool ' arping' CONFIG_USER_BUSYBOX_ARPING n
 bool ' dnsd' CONFIG_USER_BUSYBOX_DNSD n
 bool ' ether-wake' CONFIG_USER_BUSYBOX_ETHER_WAKE n
 bool ' fakeidentd' CONFIG_USER_BUSYBOX_FAKEIDENTD n
 if [ "$CONFIG_USER_BUSYBOX_FAKEIDENTD" = "y" ]; then
   if [ "$CONFIG_USER_BUSYBOX_FEATURE_SYSLOG" != "y" ]; then
     define_bool CONFIG_USER_BUSYBOX_FEATURE_SYSLOG y
   fi
 fi
 bool ' ftpget' CONFIG_USER_BUSYBOX_FTPGET n
 bool ' ftpput' CONFIG_USER_BUSYBOX_FTPPUT n
 if [ "$CONFIG_USER_BUSYBOX_GETOPT_LONG" = "y" -a "$CONFIG_USER_BUSYBOX_FTPGET" = "y" -o "$CONFIG_USER_BUSYBOX_GETOPT_LONG" = "y" -a "$CONFIG_USER_BUSYBOX_FTPPUT" = "y" ]; then
   bool '   Enable long options in ftpget/ftpput' CONFIG_USER_BUSYBOX_FEATURE_FTPGETPUT_LONG_OPTIONS n
 fi
 bool ' hostname' CONFIG_USER_BUSYBOX_HOSTNAME n
 bool ' httpd' CONFIG_USER_BUSYBOX_HTTPD n
 if [ "$CONFIG_USER_BUSYBOX_HTTPD" = "y" ]; then
   bool '   Support reloading the global config file using hup signal' CONFIG_USER_BUSYBOX_FEATURE_HTTPD_RELOAD_CONFIG_SIGHUP n
 fi
 if [ "$CONFIG_USER_BUSYBOX_HTTPD" = "y" ]; then
   bool '   Enable -u <user> option' CONFIG_USER_BUSYBOX_FEATURE_HTTPD_SETUID n
 fi
 if [ "$CONFIG_USER_BUSYBOX_HTTPD" = "y" ]; then
   bool '   Enable Basic http Authentication' CONFIG_USER_BUSYBOX_FEATURE_HTTPD_BASIC_AUTH y
 fi
 if [ "$CONFIG_USER_BUSYBOX_FEATURE_HTTPD_BASIC_AUTH" = "y" ]; then
   bool '   Support MD5 crypted passwords for http Authentication' CONFIG_USER_BUSYBOX_FEATURE_HTTPD_AUTH_MD5 n
 fi
 if [ "$CONFIG_USER_BUSYBOX_HTTPD" = "y" ]; then
   bool '   Support loading additional MIME types at run-time' CONFIG_USER_BUSYBOX_FEATURE_HTTPD_CONFIG_WITH_MIME_TYPES n
 fi
 if [ "$CONFIG_USER_BUSYBOX_HTTPD" = "y" ]; then
   bool '   Support Common Gateway Interface (CGI)' CONFIG_USER_BUSYBOX_FEATURE_HTTPD_CGI y
 fi
 if [ "$CONFIG_USER_BUSYBOX_FEATURE_HTTPD_CGI" = "y" ]; then
   bool '   Enable support for running scripts through an interpreter' CONFIG_USER_BUSYBOX_FEATURE_HTTPD_CONFIG_WITH_SCRIPT_INTERPR n
 fi
 if [ "$CONFIG_USER_BUSYBOX_FEATURE_HTTPD_CGI" = "y" ]; then
   bool '   Support the REMOTE_PORT environment variable for CGI' CONFIG_USER_BUSYBOX_FEATURE_HTTPD_SET_REMOTE_PORT_TO_ENV n
 fi
 if [ "$CONFIG_USER_BUSYBOX_HTTPD" = "y" ]; then
   bool '   Enable the -e option for shell script CGI simplification.' CONFIG_USER_BUSYBOX_FEATURE_HTTPD_ENCODE_URL_STR y
 fi
 bool ' ifconfig' CONFIG_USER_BUSYBOX_IFCONFIG n
 if [ "$CONFIG_USER_BUSYBOX_IFCONFIG" = "y" ]; then
   bool '   Enable status reporting output (+7k)' CONFIG_USER_BUSYBOX_FEATURE_IFCONFIG_STATUS y
 fi
 if [ "$CONFIG_USER_BUSYBOX_IFCONFIG" = "y" ]; then
   bool '   Enable slip-specific options "keepalive" and "outfill"' CONFIG_USER_BUSYBOX_FEATURE_IFCONFIG_SLIP n
 fi
 if [ "$CONFIG_USER_BUSYBOX_IFCONFIG" = "y" ]; then
   bool '   Enable options "mem_start", "io_addr", and "irq"' CONFIG_USER_BUSYBOX_FEATURE_IFCONFIG_MEMSTART_IOADDR_IRQ n
 fi
 if [ "$CONFIG_USER_BUSYBOX_IFCONFIG" = "y" ]; then
   bool '   Enable option "hw" (ether only)' CONFIG_USER_BUSYBOX_FEATURE_IFCONFIG_HW y
 fi
 if [ "$CONFIG_USER_BUSYBOX_IFCONFIG" = "y" ]; then
   bool '   Set the broadcast automatically' CONFIG_USER_BUSYBOX_FEATURE_IFCONFIG_BROADCAST_PLUS n
 fi
 bool ' ifupdown' CONFIG_USER_BUSYBOX_IFUPDOWN n
 if [ "$CONFIG_USER_BUSYBOX_IFUPDOWN" = "y" ]; then
   if [ "$CONFIG_USER_BUSYBOX_RUN_PARTS" != "y" ]; then
     define_bool CONFIG_USER_BUSYBOX_RUN_PARTS y
   fi
 fi
 if [ "$CONFIG_USER_BUSYBOX_IFUPDOWN" = "y" ]; then
   bool '   Use ip applet' CONFIG_USER_BUSYBOX_FEATURE_IFUPDOWN_IP n
 fi
 if [ "$CONFIG_USER_BUSYBOX_FEATURE_IFUPDOWN_IP" = "y" ]; then
   bool '   Use busybox ip applet' CONFIG_USER_BUSYBOX_FEATURE_IFUPDOWN_IP_BUILTIN y
   if [ "$CONFIG_USER_BUSYBOX_FEATURE_IFUPDOWN_IP_BUILTIN" = "y" ]; then
     if [ "$CONFIG_USER_BUSYBOX_FEATURE_IP_ROUTE" != "y" ]; then
       define_bool CONFIG_USER_BUSYBOX_FEATURE_IP_ROUTE y
     fi
     if [ "$CONFIG_USER_BUSYBOX_IP" != "y" ]; then
       define_bool CONFIG_USER_BUSYBOX_IP y
     fi
     if [ "$CONFIG_USER_BUSYBOX_FEATURE_IP_ADDRESS" != "y" ]; then
       define_bool CONFIG_USER_BUSYBOX_FEATURE_IP_ADDRESS y
     fi
     if [ "$CONFIG_USER_BUSYBOX_FEATURE_IP_LINK" != "y" ]; then
       define_bool CONFIG_USER_BUSYBOX_FEATURE_IP_LINK y
     fi
   fi
 fi
 if [ "$CONFIG_USER_BUSYBOX_IFUPDOWN" = "y" -a "$CONFIG_USER_BUSYBOX_FEATURE_IFUPDOWN_IP" != "y" ]; then
   bool '   Use busybox ifconfig and route applets' CONFIG_USER_BUSYBOX_FEATURE_IFUPDOWN_IFCONFIG_BUILTIN y
   if [ "$CONFIG_USER_BUSYBOX_FEATURE_IFUPDOWN_IFCONFIG_BUILTIN" = "y" ]; then
     if [ "$CONFIG_USER_BUSYBOX_IFCONFIG" != "y" ]; then
       define_bool CONFIG_USER_BUSYBOX_IFCONFIG y
     fi
     if [ "$CONFIG_USER_BUSYBOX_ROUTE" != "y" ]; then
       define_bool CONFIG_USER_BUSYBOX_ROUTE y
     fi
   fi
 fi
 if [ "$CONFIG_USER_BUSYBOX_IFUPDOWN" = "y" ]; then
   bool '   Enable support for IPv4' CONFIG_USER_BUSYBOX_FEATURE_IFUPDOWN_IPV4 y
 fi
 if [ "$CONFIG_USER_BUSYBOX_IFUPDOWN" = "y" -a "$CONFIG_USER_BUSYBOX_FEATURE_IPV6" = "y" ]; then
   bool '   Enable support for IPv6' CONFIG_USER_BUSYBOX_FEATURE_IFUPDOWN_IPV6 n
 fi
 if [ "$CONFIG_USER_BUSYBOX_IFUPDOWN" = "y" ]; then
   bool '   Enable support for IPX' CONFIG_USER_BUSYBOX_FEATURE_IFUPDOWN_IPX n
 fi
 if [ "$CONFIG_USER_BUSYBOX_IFUPDOWN" = "y" ]; then
   bool '   Enable mapping support' CONFIG_USER_BUSYBOX_FEATURE_IFUPDOWN_MAPPING n
 fi
 bool ' inetd' CONFIG_USER_BUSYBOX_INETD n
 if [ "$CONFIG_USER_BUSYBOX_INETD" = "y" ]; then
   if [ "$CONFIG_USER_BUSYBOX_FEATURE_SYSLOG" != "y" ]; then
     define_bool CONFIG_USER_BUSYBOX_FEATURE_SYSLOG y
   fi
 fi
 if [ "$CONFIG_USER_BUSYBOX_INETD" = "y" ]; then
   bool '   Support echo service' CONFIG_USER_BUSYBOX_FEATURE_INETD_SUPPORT_BUILTIN_ECHO y
 fi
 if [ "$CONFIG_USER_BUSYBOX_INETD" = "y" ]; then
   bool '   Support discard service' CONFIG_USER_BUSYBOX_FEATURE_INETD_SUPPORT_BUILTIN_DISCARD y
 fi
 if [ "$CONFIG_USER_BUSYBOX_INETD" = "y" ]; then
   bool '   Support time service' CONFIG_USER_BUSYBOX_FEATURE_INETD_SUPPORT_BUILTIN_TIME y
 fi
 if [ "$CONFIG_USER_BUSYBOX_INETD" = "y" ]; then
   bool '   Support daytime service' CONFIG_USER_BUSYBOX_FEATURE_INETD_SUPPORT_BUILTIN_DAYTIME y
 fi
 if [ "$CONFIG_USER_BUSYBOX_INETD" = "y" ]; then
   bool '   Support chargen service' CONFIG_USER_BUSYBOX_FEATURE_INETD_SUPPORT_BUILTIN_CHARGEN y
 fi
 if [ "$CONFIG_USER_BUSYBOX_INETD" = "y" -a "$CONFIG_USER_BUSYBOX_FEATURE_HAVE_RPC" = "y" ]; then
   bool '   Support RPC services' CONFIG_USER_BUSYBOX_FEATURE_INETD_RPC n
 fi
 bool ' ip' CONFIG_USER_BUSYBOX_IP n
 if [ "$CONFIG_USER_BUSYBOX_IP" = "y" ]; then
   bool '   ip address' CONFIG_USER_BUSYBOX_FEATURE_IP_ADDRESS y
 fi
 if [ "$CONFIG_USER_BUSYBOX_IP" = "y" ]; then
   bool '   ip link' CONFIG_USER_BUSYBOX_FEATURE_IP_LINK y
 fi
 if [ "$CONFIG_USER_BUSYBOX_IP" = "y" ]; then
   bool '   ip route' CONFIG_USER_BUSYBOX_FEATURE_IP_ROUTE y
 fi
 if [ "$CONFIG_USER_BUSYBOX_IP" = "y" ]; then
   bool '   ip tunnel' CONFIG_USER_BUSYBOX_FEATURE_IP_TUNNEL n
 fi
 if [ "$CONFIG_USER_BUSYBOX_IP" = "y" ]; then
   bool '   ip rule' CONFIG_USER_BUSYBOX_FEATURE_IP_RULE n
 fi
 if [ "$CONFIG_USER_BUSYBOX_IP" = "y" ]; then
   bool '   Support short forms of ip commands.' CONFIG_USER_BUSYBOX_FEATURE_IP_SHORT_FORMS n
 fi
 if [ "$CONFIG_USER_BUSYBOX_FEATURE_IP_SHORT_FORMS" = "y" -a "$CONFIG_USER_BUSYBOX_FEATURE_IP_ADDRESS" = "y" ]; then
    if [ "$CONFIG_USER_BUSYBOX_IPADDR" != "y" ]; then
      define_bool CONFIG_USER_BUSYBOX_IPADDR y
    fi
 fi
 if [ "$CONFIG_USER_BUSYBOX_FEATURE_IP_SHORT_FORMS" = "y" -a "$CONFIG_USER_BUSYBOX_FEATURE_IP_LINK" = "y" ]; then
    if [ "$CONFIG_USER_BUSYBOX_IPLINK" != "y" ]; then
      define_bool CONFIG_USER_BUSYBOX_IPLINK y
    fi
 fi
 if [ "$CONFIG_USER_BUSYBOX_FEATURE_IP_SHORT_FORMS" = "y" -a "$CONFIG_USER_BUSYBOX_FEATURE_IP_ROUTE" = "y" ]; then
    if [ "$CONFIG_USER_BUSYBOX_IPROUTE" != "y" ]; then
      define_bool CONFIG_USER_BUSYBOX_IPROUTE y
    fi
 fi
 if [ "$CONFIG_USER_BUSYBOX_FEATURE_IP_SHORT_FORMS" = "y" -a "$CONFIG_USER_BUSYBOX_FEATURE_IP_TUNNEL" = "y" ]; then
    if [ "$CONFIG_USER_BUSYBOX_IPTUNNEL" != "y" ]; then
      define_bool CONFIG_USER_BUSYBOX_IPTUNNEL y
    fi
 fi
 if [ "$CONFIG_USER_BUSYBOX_FEATURE_IP_SHORT_FORMS" = "y" -a "$CONFIG_USER_BUSYBOX_FEATURE_IP_RULE" = "y" ]; then
    if [ "$CONFIG_USER_BUSYBOX_IPRULE" != "y" ]; then
      define_bool CONFIG_USER_BUSYBOX_IPRULE y
    fi
 fi
 bool ' ipcalc' CONFIG_USER_BUSYBOX_IPCALC n
 if [ "$CONFIG_USER_BUSYBOX_IPCALC" = "y" ]; then
   bool '   Fancy IPCALC, more options, adds 1 kbyte' CONFIG_USER_BUSYBOX_FEATURE_IPCALC_FANCY y
 fi
 if [ "$CONFIG_USER_BUSYBOX_IPCALC" = "y" -a "$CONFIG_USER_BUSYBOX_GETOPT_LONG" = "y" ]; then
   bool '   Enable long options' CONFIG_USER_BUSYBOX_FEATURE_IPCALC_LONG_OPTIONS n
 fi
 bool ' nameif' CONFIG_USER_BUSYBOX_NAMEIF n
 if [ "$CONFIG_USER_BUSYBOX_NAMEIF" = "y" ]; then
   if [ "$CONFIG_USER_BUSYBOX_FEATURE_SYSLOG" != "y" ]; then
     define_bool CONFIG_USER_BUSYBOX_FEATURE_SYSLOG y
   fi
 fi
 bool ' nc' CONFIG_USER_BUSYBOX_NC n
 if [ "$CONFIG_USER_BUSYBOX_NC" = "y" ]; then
   bool '   Netcat server options (-lp)' CONFIG_USER_BUSYBOX_NC_SERVER n
 fi
 if [ "$CONFIG_USER_BUSYBOX_NC" = "y" ]; then
   bool '   Netcat extensions (-eiw and filename)' CONFIG_USER_BUSYBOX_NC_EXTRA n
 fi
 bool ' netstat' CONFIG_USER_BUSYBOX_NETSTAT n
 bool ' nslookup' CONFIG_USER_BUSYBOX_NSLOOKUP n
 bool ' ping' CONFIG_USER_BUSYBOX_PING n
 if [ "$CONFIG_USER_BUSYBOX_PING" = "y" ]; then
   bool '   Enable fancy ping output' CONFIG_USER_BUSYBOX_FEATURE_FANCY_PING y
 fi
 if [ "$CONFIG_USER_BUSYBOX_FEATURE_IPV6" = "y" ]; then
   bool '   ping6' CONFIG_USER_BUSYBOX_PING6 n
 fi
 if [ "$CONFIG_USER_BUSYBOX_PING6" = "y" ]; then
   bool '   Enable fancy ping6 output' CONFIG_USER_BUSYBOX_FEATURE_FANCY_PING6 y
 fi
 bool ' route' CONFIG_USER_BUSYBOX_ROUTE n
 bool ' telnet' CONFIG_USER_BUSYBOX_TELNET n
 if [ "$CONFIG_USER_BUSYBOX_TELNET" = "y" ]; then
   bool '   Pass TERM type to remote host' CONFIG_USER_BUSYBOX_FEATURE_TELNET_TTYPE y
 fi
 if [ "$CONFIG_USER_BUSYBOX_TELNET" = "y" ]; then
   bool '   Pass USER type to remote host' CONFIG_USER_BUSYBOX_FEATURE_TELNET_AUTOLOGIN y
 fi
 bool ' telnetd' CONFIG_USER_BUSYBOX_TELNETD n
 if [ "$CONFIG_USER_BUSYBOX_TELNETD" = "y" ]; then
   if [ "$CONFIG_USER_BUSYBOX_FEATURE_SYSLOG" != "y" ]; then
     define_bool CONFIG_USER_BUSYBOX_FEATURE_SYSLOG y
   fi
 fi
 if [ "$CONFIG_USER_BUSYBOX_TELNETD" = "y" ]; then
   bool '   Support standalone telnetd (not inetd only)' CONFIG_USER_BUSYBOX_FEATURE_TELNETD_STANDALONE n
 fi
 bool ' tftp' CONFIG_USER_BUSYBOX_TFTP n
 if [ "$CONFIG_USER_BUSYBOX_TFTP" = "y" ]; then
   bool '   Enable "get" command' CONFIG_USER_BUSYBOX_FEATURE_TFTP_GET y
 fi
 if [ "$CONFIG_USER_BUSYBOX_TFTP" = "y" ]; then
   bool '   Enable "put" command' CONFIG_USER_BUSYBOX_FEATURE_TFTP_PUT y
 fi
 if [ "$CONFIG_USER_BUSYBOX_TFTP" = "y" ]; then
   bool '   Enable "blocksize" command' CONFIG_USER_BUSYBOX_FEATURE_TFTP_BLOCKSIZE n
 fi
 if [ "$CONFIG_USER_BUSYBOX_TFTP" = "y" ]; then
   bool '   Enable debug' CONFIG_USER_BUSYBOX_DEBUG_TFTP n
 fi
 bool ' traceroute' CONFIG_USER_BUSYBOX_TRACEROUTE n
 if [ "$CONFIG_USER_BUSYBOX_TRACEROUTE" = "y" ]; then
   bool '   Enable verbose output' CONFIG_USER_BUSYBOX_FEATURE_TRACEROUTE_VERBOSE n
 fi
 if [ "$CONFIG_USER_BUSYBOX_TRACEROUTE" = "y" ]; then
   bool '   Enable loose source route' CONFIG_USER_BUSYBOX_FEATURE_TRACEROUTE_SOURCE_ROUTE n
 fi
 if [ "$CONFIG_USER_BUSYBOX_TRACEROUTE" = "y" ]; then
   bool '   Use ICMP instead of UDP' CONFIG_USER_BUSYBOX_FEATURE_TRACEROUTE_USE_ICMP n
 fi
source ../user/busybox/uclinux-configs/networking/udhcp/Config.in
 bool ' vconfig' CONFIG_USER_BUSYBOX_VCONFIG n
 bool ' wget' CONFIG_USER_BUSYBOX_WGET n
 if [ "$CONFIG_USER_BUSYBOX_WGET" = "y" ]; then
   bool '   Enable a nifty process meter (+2k)' CONFIG_USER_BUSYBOX_FEATURE_WGET_STATUSBAR y
 fi
 if [ "$CONFIG_USER_BUSYBOX_WGET" = "y" ]; then
   bool '   Enable HTTP authentication' CONFIG_USER_BUSYBOX_FEATURE_WGET_AUTHENTICATION y
 fi
 if [ "$CONFIG_USER_BUSYBOX_WGET" = "y" -a "$CONFIG_USER_BUSYBOX_FEATURE_IPV6" = "y" ]; then
   bool '   Enable IPv6 literal addresses' CONFIG_USER_BUSYBOX_FEATURE_WGET_IP6_LITERAL y
 fi
 if [ "$CONFIG_USER_BUSYBOX_WGET" = "y" -a "$CONFIG_USER_BUSYBOX_GETOPT_LONG" = "y" ]; then
   bool '   Enable long options' CONFIG_USER_BUSYBOX_FEATURE_WGET_LONG_OPTIONS n
 fi
 bool ' zcip' CONFIG_USER_BUSYBOX_ZCIP n
 if [ "$CONFIG_USER_BUSYBOX_ZCIP" = "y" ]; then
   if [ "$CONFIG_USER_BUSYBOX_FEATURE_SYSLOG" != "y" ]; then
     define_bool CONFIG_USER_BUSYBOX_FEATURE_SYSLOG y
   fi
 fi
