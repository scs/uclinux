###
### DO NOT EDIT THIS FILE; IT IS AUTO-GENERATED
### SEE kconfig-to-uclinux.awk FOR MORE INFO
###

comment '--###-- SYSTEM LOGGING UTILITIES --###--'
 bool ' syslogd' CONFIG_USER_BUSYBOX_SYSLOGD n
 if [ "$CONFIG_USER_BUSYBOX_SYSLOGD" = "y" ]; then
   bool '   Rotate message files' CONFIG_USER_BUSYBOX_FEATURE_ROTATE_LOGFILE n
 fi
 if [ "$CONFIG_USER_BUSYBOX_SYSLOGD" = "y" ]; then
   bool '   Remote Log support' CONFIG_USER_BUSYBOX_FEATURE_REMOTE_LOG n
 fi
 if [ "$CONFIG_USER_BUSYBOX_SYSLOGD" = "y" ]; then
   bool '   Circular Buffer support' CONFIG_USER_BUSYBOX_FEATURE_IPC_SYSLOG n
 fi
 if [ "$CONFIG_USER_BUSYBOX_FEATURE_IPC_SYSLOG" = "y" ]; then
   int '       Circular buffer size in Kbytes (minimum 4KB)' CONFIG_USER_BUSYBOX_FEATURE_IPC_SYSLOG_BUFFER_SIZE 16
 fi
 if [ "$CONFIG_USER_BUSYBOX_FEATURE_IPC_SYSLOG" = "y" ]; then
   bool '   logread' CONFIG_USER_BUSYBOX_LOGREAD y
 fi
 if [ "$CONFIG_USER_BUSYBOX_LOGREAD" = "y" ]; then
   bool '   logread double buffering' CONFIG_USER_BUSYBOX_FEATURE_LOGREAD_REDUCED_LOCKING n
 fi
 if [ "$CONFIG_USER_BUSYBOX_SYSLOGD" = "y" ]; then
   bool '   klogd' CONFIG_USER_BUSYBOX_KLOGD n
   if [ "$CONFIG_USER_BUSYBOX_KLOGD" = "y" ]; then
     if [ "$CONFIG_USER_BUSYBOX_FEATURE_SYSLOG" != "y" ]; then
       define_bool CONFIG_USER_BUSYBOX_FEATURE_SYSLOG y
     fi
   fi
 fi
 bool ' logger' CONFIG_USER_BUSYBOX_LOGGER n
 if [ "$CONFIG_USER_BUSYBOX_LOGGER" = "y" ]; then
   if [ "$CONFIG_USER_BUSYBOX_FEATURE_SYSLOG" != "y" ]; then
     define_bool CONFIG_USER_BUSYBOX_FEATURE_SYSLOG y
   fi
 fi
