* add Netbios lookup, session ID and encryption support to SMB module

* add HTTP NTLM + MD5 authentication

* add pcanywhere

* add rsh, rlogin

* add oracle, ms-sql, mysql

* add SSH v1+v2

* add SNMP, RIP, OSPF, BGP
