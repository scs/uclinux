zaptel 1.0.9.1
 -- continue fxo operation after the magical 25 days
zaptel 1.0.9
 -- add a pci id for rev I tdm cards
zaptel 1.0.8 
 -- add support for 2nd gen hardware
 -- added some use counts to prevent kernel panics when improperly unloading modules
 -- fixed a problem in the Makefile that would cause problems if the system did not
    have 'chkconfig'
 -- fixed a problem with ztcfg ignoring the first number in the span= line
zaptel 1.0.7
 -- Makefile
    -- An alias has been added so that you can load wcfxs with 'modprobe wctdm'.
 -- wcte11xp
    -- Some additional subvendor IDs have been added.
 -- zaptel.init
    -- wcte11xp has been added to the list of modules in this file.
zaptel 1.0.6
 -- zaptel.init
    -- Added a wait time to the init script so that software that creates the device nodes dynamically
       has time to create the zaptel devnode.
 -- wcfxs
    -- The ringing threshold was changed to not detect a pulse dial phone as ringing on an FXO.
zaptel 1.0.4
 -- wcfxs
    -- fix a compilation issue
    -- handle module parameters using module_param for 2.6 kernels
 -- fxstest
    -- sleep after ringing
 -- zaptel.init
    -- wait for /dev/zap
 -- Makefile fix for SELinux
zaptel 1.0.3
 -- Fix Makefile to install in arbitary locations

...
0.1.6:
	* Move network structures to be malloc()'d when needed
	* Add HDLC PPP Support
	* Fix multi-channel stuff in zaptel and torisa
